# Normalize line endings. Target is Linux, so all shipped text stays LF.
* text=auto eol=lf

# Windows-only helper may use CRLF.
*.ps1 text eol=crlf

# Binary assets (icons, etc.)
*.png binary
*.ico binary
*.svg text eol=lf
